apiVersion: v1
kind: Service
metadata:
  name: {{ include "goapiproject.fullname" . }}
  labels:
    {{- include "goapiproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goapiproject.selectorLabels" . | nindent 4 }}
